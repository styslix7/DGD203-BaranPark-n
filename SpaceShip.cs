namespace DGD203.Project.BaranParkin
{
    public class SpaceShip : Car
    {
        //inheriting from Car class, just adding more features for spaceship
        public SpaceShip(string model, int year) : base(model, year)
        {
            //calling the base class constructor to set model and year
        }
    }
}